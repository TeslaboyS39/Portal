<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f0f0;
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            margin-top: 50px;
        }

        nav {
            background-color: #333;
            padding: 10px;
            display: flex;
            justify-content: center;
        }

        nav a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
            padding: 5px 10px;
            border-radius: 5px;
        }

        nav a:hover {
            background-color: #555;
        }

        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }

        table {
            font-family: arial, sans-serif;
            border-collapse: collapse;
            width: 100%;
        }

        td, th {
            border: 1px solid #dddddd;
            text-align: left;
            padding: 8px;
        }

        tr:nth-child(even) {
            background-color: #dddddd;  
        }

        a {
            text-decoration: none;
            color: black;
        }
    </style>
</head>
<body>
    <nav>
        <a href="/userProfile">Your Profile</a>
        <a href="/transactionsList">My Cash Flow</a>
        <a href="/addCategory">Add Category</a>
        <a href="/insight">Insight</a>
        <a href="/logout">Logout</a>
    </nav>
    <h1>User Profile</h1>
    <% if (user.UserDetail) { %>
        <p>User Name: <%= user.UserDetail.userName %></p> <br>
        <p>User Birth Date: <%= user.UserDetail.userBirthDate %></p><br>
        <p><img src="<%= user.UserDetail.userProfilePicture %>" alt="Profile Picture" width="150"></p><br>
        <p>Balance: <%= user.UserDetail.balance %></p><br>
        <a href="/editProfile">Edit</a>
    <% } else { %>
        <p>User profile not available.</p>
        <a href="/addProfile">Add</a>
    <% } %>
</body>
</html>